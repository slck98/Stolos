import React, { Suspense } from "react";


const EditGascard = () => {
  return (
    <div>dgd</div>
    // <Suspense>
    //     <Await resolve={gascard}>

    // </Suspense>
  );
};

export default EditGascard;
